package com.example.carwashautospa.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta Principal (Car Wash Theme)
val PrimaryBlue = Color(0xFF0061A4)
val PrimaryBlueDark = Color(0xFF001D36)
val SecondaryCyan = Color(0xFF00687A)
val AccentAqua = Color(0xFF4DD0E1)

// Colores Neutros
val BackgroundLight = Color(0xFFF8FDFF)
val SurfaceLight = Color(0xFFFFFFFF)
val CardBorder = Color(0xFFE0E3E5)

// Colores de Estado de Atención
val EstadoPendiente = Color(0xFFE65100)  // Naranja
val EstadoEnProceso = Color(0xFF0288D1)  // Azul Informátivo
val EstadoListo = Color(0xFF2E7D32)      // Verde Exitoso
val EstadoCancelado = Color(0xFFC62828)  // Rojo Error